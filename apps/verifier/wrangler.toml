# Cloudflare Pages configuration for Verifier App
name = "proofchain-verifier"
compatibility_date = "2024-01-01"
pages_build_output_dir = ".next"

[vars]
NEXT_PUBLIC_APP_NAME = "ProofChain Verifier"
NEXT_PUBLIC_BLOCKFROST_NETWORK = "preprod"
